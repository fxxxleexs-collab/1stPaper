"symbolic_music_features" 六个特征家族说明

一、整体思路：先把乐谱变成“可统计的事件表”

这个项目的核心思路是：不要直接在完整乐谱对象上做复杂判断，而是先把 MusicXML / MXL / XML 乐谱转换成一张标准化的事件表。之后所有特征都从这张表里统计。

可以简单理解为：

乐谱文件
→ music21 Score
→ 标准化事件表
→ 按小节切成片段
→ 每个片段提取特征
→ 汇总成作品层面或时期层面的结果

事件表里的每一行大致代表一个音乐事件。音符和休止符会直接保留；和弦会被拆成多个音高行，但仍然保留“这是和弦”的标记。这样做的好处是：音高、音程、纵向和声、节奏、织体等特征都可以从同一套基础数据中提取。

不过，这里有一个重要限制：代码里的 "voice_id" 是乐谱编码层面的声部编号，不一定等同于音乐分析意义上的真实声部。因此，所有基于声部的特征都应该理解为“编码声部代理特征”，而不是严格的声部分离结果。

---

二、音高特征：这段音乐用了哪些音、音区在哪里

音高特征主要回答：

«这段音乐的音域有多宽？整体音高偏高还是偏低？使用了哪些音级或音高类别？最高点出现在片段的什么位置？»

它关注的是音乐的“音高空间”，而不是完整和声分析。

1. 音符数量

代码会统计一个片段里有多少个带音高的音符行，即：

pitch_count

因为和弦会被拆成多个音高行，所以一个三和弦会贡献三个音高行。这个数字主要用于诊断，不适合作为最重要的风格判断依据，因为它很容易受到作品密度和记谱方式影响。

2. 音域与音高分布

代码会统计：

pitch_range
pitch_mean
pitch_std
pitch_min
pitch_max

这些特征分别表示：

- 音域跨度；
- 平均音高；
- 音高分布是否分散；
- 最低音；
- 最高音。

音乐学上可以用来观察：某类作品是否更集中在中音区，某些时期是否音域更开阔，或者某些体裁是否更偏高音/低音活动。

3. 十二个音级的使用比例

代码会把所有音高转换成 12 个 pitch class，也就是：

C, C#, D, ..., B

然后统计每个 pitch class 的比例：

pitch_class_0
pitch_class_1
...
pitch_class_11

同时还会计算：

pitch_class_entropy

这个 entropy 可以理解为“音高类别使用得是否分散”。如果音乐总是在少数几个 pitch class 附近活动，entropy 会低；如果十二个 pitch class 使用得更均匀，entropy 会高。

注意：这不是完整的调性分析，它只是统计音级分布。

4. 局部最高点的位置

代码还会找到片段中的最高音，并记录这个最高音第一次出现的位置：

climax_relative_position

如果这个值接近 0，说明最高点在片段开头附近；如果接近 1，说明最高点在片段末尾附近。

这可以粗略观察音乐的局部形态，比如某些片段是否有明显向高潮推进的趋势。

---

三、音程特征：旋律和声部如何运动

音程特征主要回答：

«音与音之间是级进、跳进，还是大跳？旋律方向是否经常变化？低音线和高音线的运动有什么特点？纵向同时发声的音之间是什么关系？»

这个家族比单纯音高特征更接近“音乐运动方式”。

代码里把音程分成两大类：

线性音程
纵向音程

---

1. 线性音程：相邻音之间怎么走

线性音程有三个来源：

encoded_voice
top_line
bass_line

它们分别表示：

- "encoded_voice"：按乐谱编码里的 part 和 voice 分组，统计同一编码声部内相邻音之间的距离；
- "top_line"：每个 onset 取最高音，形成一个上方轮廓线；
- "bass_line"：每个 onset 取最低音，形成一个低音轮廓线。

这三者都不是严格的旋律还原，而是为了给音乐分析提供可稳定统计的运动代理。

2. 线性音程具体统计什么

对每一种线性来源，代码都会统计类似下面这些指标：

repeat_ratio
step_ratio
small_leap_ratio
medium_leap_ratio
large_leap_ratio
ascending_ratio
descending_ratio
direction_change_rate
interval_entropy
mean_abs_interval

它们的含义大致是：

- "repeat_ratio"：同音反复比例；
- "step_ratio"：一度、二度级进比例；
- "small_leap_ratio"：三度、四度小跳比例；
- "medium_leap_ratio"：五度、六度中等跳进比例；
- "large_leap_ratio"：七度及以上大跳比例；
- "ascending_ratio"：上行比例；
- "descending_ratio"：下行比例；
- "direction_change_rate"：方向变化频率；
- "interval_entropy"：音程类型是否丰富；
- "mean_abs_interval"：平均音程距离。

音乐学上，这些特征可以帮助判断一段音乐是更“歌唱性”、更“器乐化”、更“幻想性”，还是更“规整模进化”。

例如：

- 级进比例高，可能说明线条更顺阶、更连续；
- 大跳比例高，可能说明线条更跳跃、更器乐化或更幻想性；
- 方向变化率高，可能说明旋律轮廓更曲折；
- 低音线的五度、四度运动较多，可能提示某种低音进行或和声组织特征。

---

3. 音程直方图：具体哪些音程出现得多

代码还会为 top line 和 bass line 输出从 "-12" 到 "+12" 的方向性音程比例：

interval_top_line_hist_-12 ... interval_top_line_hist_12
interval_bass_line_hist_-12 ... interval_bass_line_hist_12

这相当于更细的音程分布图。
例如，"+2" 多说明上行二度多，"-2" 多说明下行二度多，"+7" 多说明上行五度多。

这种特征适合观察某些作曲家或时期是否偏好某些局部运动公式。

---

4. 纵向音程：同一时刻同时响起的音之间是什么关系

代码会按 "global_onset_ql" 把同一时间点出现的音聚在一起，然后计算两两之间的 interval class。

它会输出：

vertical_ic_0_ratio ... vertical_ic_11_ratio

也会把这些 interval class 合并成更容易解释的类别：

vertical_unison_octave_ratio
vertical_second_seventh_ratio
vertical_third_sixth_ratio
vertical_fourth_fifth_ratio
vertical_tritone_ratio

这些特征可以帮助观察纵向音响结构。例如：

- 三度、六度比例较高，可能更偏和声性、协和性；
- 二度、七度比例较高，可能说明局部不协和或经过性音响较多；
- 四度、五度比例较高，可能与低音框架、空五度、对位结构有关；
- 三全音比例可以作为紧张音响的粗略代理。

代码还会统计：

vertical_interval_count
vertical_interval_count_per_onset
vertical_interval_entropy

其中 "vertical_interval_count_per_onset" 比 raw count 更适合建模，因为它考虑了 onset 数量作为分母。

需要注意的是：当前纵向音程是 onset-based，也就是只看音符开始的时间点，不做完整的延音音响追踪。因此它不是严格的 sounding sonority 分析。

---

四、节奏特征：这段音乐的运动感和时值结构

节奏特征主要回答：

«音符事件有多密？时值是否多样？是否有附点节奏？是否有弱拍上的长音？音乐运动是否更规整或更有推动力？»

1. 为什么节奏统计前要先去重

由于和弦在事件表中会被拆成多个音高行，如果直接数行数，和弦会被误认为多个节奏事件。

因此，节奏特征会先使用去重逻辑，把同一个声部中同一 onset、同一 duration 的和弦重新视为一个节奏事件。

简单说：

和弦的多个音高行
→ 节奏统计时算作一个事件

这样节奏特征不会被和弦拆分污染。

---

2. 事件数量与密度

代码会统计：

rhythm_event_count
rhythm_note_density_per_ql

其中：

- "rhythm_event_count" 是去重后的节奏事件数，主要用于诊断；
- "rhythm_note_density_per_ql" 是单位时间内的非休止事件数量，更适合比较不同片段。

音乐学上，这可以对应“运动密度”或“音型活动量”。

例如，意大利协奏曲式的 motor rhythm 往往会体现为较高、较稳定的节奏活动。

---

3. 休止与时值分布

代码会统计：

rhythm_rest_ratio
rhythm_duration_mean
rhythm_duration_std
rhythm_duration_entropy

它们分别表示：

- 休止比例；
- 平均时值；
- 时值变化幅度；
- 时值类型是否多样。

"duration_entropy" 高，说明时值种类更丰富；低则说明节奏更单一或更规整。

---

4. 附点节奏

代码会统计：

rhythm_dotted_ratio

它通过一组常见附点时值来判断某个 duration 是否像附点节奏。

这个指标适合粗略观察法国式附点、舞曲性节奏、序曲性节奏等倾向。不过它只是启发式判断，不是完整的节奏记谱分析。

---

5. 弱拍长音代理

代码还会统计：

rhythm_beat_strength_mean
rhythm_syncopation_proxy

其中 "rhythm_syncopation_proxy" 的逻辑比较简单：

如果事件开始在较弱拍上，并且持续时间不短，就把它视为 syncopation-like

具体来说，它会找 "beat_strength < 0.5" 的事件，再看其中 "duration_ql >= 1.0" 的比例。

这可以粗略反映“弱拍上出现较长持续”的现象，但不能等同于完整的切分音理论。它不分析跨小节连音链，也不处理复杂的节拍错位。

---

五、调性特征：不用完整和声分析，但观察调性倾向

调性特征主要回答：

«这段音乐大致处在什么调性环境？音级使用是否集中？半音化程度如何？局部音级分布是否频繁变化？»

这个家族刻意避免做复杂罗马数字分析或完整转调识别，而是使用更稳定、可批量运行的轻量代理。

---

1. 调号和大小调上下文

事件表中会保留：

key_signature
key_signature_fifths
key_mode

代码会统计：

tonal_key_signature_fifths_mean
tonal_major_key_ratio
tonal_minor_key_ratio

它们大致表示：

- 片段所在调号平均有多少升降号；
- major 上下文比例；
- minor 上下文比例。

注意：代码更信任 "key_signature_fifths" 这种调号信息，而不是完全依赖乐谱里可能存在的分析性 key label。

---

2. 音级分布熵

代码会统计：

tonal_pitch_class_entropy

它也是基于 pitch class 的分布，但放在 tonal family 中时，更强调“调性材料是否集中或分散”。

如果 pitch class entropy 高，说明音级使用比较分散；如果低，说明音级更集中。

---

3. 半音化比例

代码会统计：

tonal_chromaticism_ratio

大致方法是：

1. 根据当前调号和大小调模式，估计一个 diatonic pitch-class set；
2. 看每个音是否落在这个调内集合之外；
3. 统计调外音比例。

音乐学上，这可以作为半音化、局部离调或非调内音使用的粗略指标。

但它有一个重要限制：小调中的升导音、旋律小调变化等不会被完整建模，所以它不能直接等同于真正的和声功能分析。

---

4. 局部调性变化代理

代码还会统计：

tonal_local_key_diversity_proxy

它的做法是：

1. 把片段按固定长度窗口切开；
2. 每个窗口统计 pitch-class histogram；
3. 比较相邻窗口的 pitch-class 分布差异；
4. 用平均差异表示局部调性材料变化程度。

如果这个值较高，说明片段内部不同位置的音级分布变化比较明显。
这可能对应转调、离调、主题材料变化或局部音型改变。

但它不是严格的 modulation count。它只是“局部音级分布变化”的代理。

---

六、复调与织体特征：声部厚度、纵向音响和运动独立性

复调与织体特征主要回答：

«这段音乐有多厚？几个声部同时活动？声部数量是否稳定？多个声部是一起运动，还是交错进入？纵向音响更协和还是更紧张？»

代码里这个家族包含两层：

legacy polyphony features
newer texture-oriented features

也就是说，既有比较早期的“同时发声/纵向音响”统计，也有后来新增的 active voice distribution、texture stability、voice independence proxy 等。

---

1. 同时活动声部与同时音高数量

代码会统计：

polyphony_active_voice_mean
polyphony_active_voice_max
polyphony_simultaneity_mean
polyphony_simultaneity_max

其中：

- "active_voice" 统计同一 onset 上有多少个不同的 "(part_id, voice_id)" 在活动；
- "simultaneity" 统计同一 onset 上有多少个 pitch rows，也就是同时出现多少音高。

两者不完全一样：

active_voice 更接近“编码声部厚度”
simultaneity 更接近“同时音高数量”

例如，一个声部里写了一个三和弦，simultaneity 会增加，但 active voice 不一定增加。

---

2. 和弦事件比例

代码会统计：

polyphony_chord_event_ratio

它表示 note rows 中有多少来自 chord object。

这可以粗略反映：这段音乐是否经常出现和弦式记谱，或者是否更偏单音线条。

---

3. 纵向协和/不协和代理

代码会统计：

polyphony_vertical_interval_entropy
polyphony_consonance_ratio
polyphony_dissonance_ratio

其做法是：在每个 onset 上计算同时音之间的 interval classes，再把某些 interval class 粗略归为协和或不协和。

这不是严格对位学里的协和/不协和处理分析。它不看准备、解决、经过音，也不判断和声功能，只是一个纵向音响平衡的粗略指标。

---

4. active voice distribution：声部厚度分布

新增 texture 特征会统计：

texture_active_voice_1_ratio
texture_active_voice_2_ratio
texture_active_voice_3_ratio
texture_active_voice_4_ratio
texture_active_voice_5plus_ratio
texture_active_voice_mean
texture_active_voice_std
texture_active_voice_entropy

它不是只看平均声部数，而是看一个片段中：

- 有多少 onset 是 1 个声部；
- 有多少 onset 是 2 个声部；
- 有多少 onset 是 3 个声部；
- 有多少 onset 是 4 个声部；
- 有多少 onset 是 5 个及以上声部。

这对音乐学解释很有用。比如：

- trio texture 可能长期保持 3 个声部；
- 管风琴厚织体可能出现 4 个或更多声部；
- 幻想性段落可能声部数变化很大。

---

5. texture stability：织体是否稳定

代码会统计：

texture_voice_count_change_rate
texture_voice_count_change_mean_abs
texture_voice_count_change_max_abs
texture_voice_count_change_std_abs
texture_voice_count_stability_ratio

它的做法是：按时间顺序看每个 onset 的 active voice count，然后观察相邻 onset 之间声部数是否变化、变化幅度多大。

可以理解为：

这段音乐的织体厚度是一直稳定，还是频繁变厚变薄？

例如：

- 如果一段音乐一直是 3 个声部，stability 会高；
- 如果一会儿 1 声部、一会儿 4 声部、一会儿 2 声部，change rate 和 change magnitude 会高。

---

6. homorhythmic / staggered onset 代理

代码会统计：

texture_homorhythmic_onset_ratio
texture_staggered_onset_ratio

它的逻辑是：

- 如果一个 onset 上有多个声部同时进入，就更像同步攻击；
- 如果一个 onset 上只有一个声部进入，就更像交错进入。

这不能严格判断 homophony 或 polyphony，但可以作为声部进入方式的轻量代理。

---

7. 声部运动方向代理

代码还会统计：

texture_direction_agreement_ratio
texture_direction_disagreement_ratio
texture_direction_mixed_ratio
texture_parallel_motion_ratio
texture_contrary_motion_ratio
texture_oblique_motion_ratio

它的做法是：

1. 按编码声部分组；
2. 把同一 onset 中的多个音高压缩成一个平均 pitch；
3. 计算每个声部相邻音之间是上行、下行还是重复；
4. 比较同一时间点上不同声部的运动方向。

例如：

- 两个声部都上行，算 agreement / parallel-like；
- 一个上行一个下行，算 disagreement / contrary-like；
- 一个不动，一个运动，算 oblique-like。

这只是方向层面的代理，不分析真实声部连接，也不区分严格对位学中的 similar motion 与 parallel motion。

---

七、动机与模进特征：局部语汇是否反复出现

动机与模进特征主要回答：

«这段音乐内部是否有反复出现的局部音型？是否有移调重复？是否有模进式连续？片段内部的 interval-rhythm 结构是否自相似？»

这个家族尤其适合研究意大利协奏曲式语汇、模进、局部音型复现，以及巴赫如何把局部材料发展成更大结构。

需要强调：代码不会声称自己能自动识别真正的主题或动机。它统计的是“局部符号模式的复现”。

---

1. 三种 token

代码会把音乐材料转换成 token 序列。

主要有三类：

A. interval tokens

例如：

i2
i-3
i0

表示某个编码声部中相邻音之间的音程。

B. rhythm tokens

例如：

dur_0.5
dur_1.0
dur_2.0

表示时值。时值会四舍五入到三位小数。

C. interval-rhythm cell tokens

例如：

i2_dur_0.5
i-3_dur_1.0

它把音程运动和起始音时值合在一起，因此更接近“局部音型单元”。

---

2. recurrent local pattern：重复模式比例

代码会统计：

motif_repeated_interval_bigram_ratio
motif_repeated_interval_trigram_ratio
motif_repeated_rhythm_bigram_ratio
motif_repeated_interval_rhythm_cell_ratio

它的基本公式是：

重复 n-gram 出现次数 / 全部 n-gram 出现次数

例如，一串 token 是：

A B A B C

bigram 是：

AB, BA, AB, BC

其中 "AB" 出现了两次，所以重复 bigram 比例会反映这种局部复现。

这样做比 raw unique count 更好，因为它不是简单问“有多少种模式”，而是问：

«这段音乐里有多大比例的材料属于重复出现的局部模式？»

---

3. exact repetition：完全重复

代码会统计：

motif_exact_repetition_ratio

它目前使用固定长度的 pitch trigram，也就是三个音一组，看完全相同的音高序列是否重复出现。

这可以捕捉 literal pitch recurrence，但不是长距离主题再现。

---

4. transposed repetition：移调重复代理

代码会统计：

motif_transposed_repetition_ratio

它的做法是：

1. 取长度为 3 的 pitch window；
2. 把 pitch window 转成 interval pattern；
3. 如果同一个 interval pattern 出现在不同起始音高上，就视为候选移调重复。

例如：

C-D-E  → +2, +2
D-E-F# → +2, +2

虽然绝对音高不同，但音程模式相同，所以会被视为 transposed repetition candidate。

这个特征非常适合捕捉模进性材料，因为模进经常保留音程形状，但改变音高位置。

---

5. direction-consistent pattern：方向一致的运动模式

代码会把音程简化成方向和大小类别，比如：

R
U_step
D_step
U_small_leap
D_small_leap
U_large_leap
D_large_leap

然后统计这些粗略运动形状是否重复。

这可以捕捉：

«即使具体音程不完全相同，运动方向和运动类型是否相似？»

例如连续的上行级进、小跳推进，都可能在这里体现出来。

---

6. sequence-like continuation：模进式连续代理

代码会统计：

motif_sequence_like_ratio
motif_sequence_run_mean_length
motif_sequence_run_max_length

它会看相邻的小窗口是否共享：

- 相同的音程模式；
- 或相同的方向类别模式。

如果连续多个窗口保持类似模式，就会被看作 sequence-like continuation。

这不是严格的模进识别，因为真正的模进还涉及和声、声部、调性层级等因素。
但它可以很好地作为“局部连续模式”的代理。

---

7. self-similarity：片段内部是否自相似

代码会统计：

motif_self_similarity_mean
motif_self_similarity_max
motif_self_similarity_entropy

默认使用 interval-rhythm cell tokens。它会把 token 序列切成长度为 4 的滑动窗口，然后两两比较窗口之间的 Jaccard similarity。

简单说：

如果很多窗口包含相似 token，self-similarity 就高。

三个指标分别表示：

- 平均相似度；
- 最高相似度；
- 相似度分布是否集中或分散。

这可以帮助观察片段内部是否反复使用相似的局部语汇。

---

八、几个总体解释原则

1. 这些特征是“代理”，不是完整音乐分析

项目的设计不是要让机器替代音乐学分析，而是把一些传统上需要人工感知的风格现象，转化成可以批量比较的统计线索。

例如：

encoded voice motion ≠ 真正声部分离
top line ≠ 真正旋律
vertical interval ≠ 完整和声分析
motif repetition ≠ 真正主题识别
syncopation proxy ≠ 完整节奏理论

这些特征应该被理解为“可以帮助发现趋势的指标”。

---

2. 尽量用比例，而不是原始数量

项目里很多 raw count 会保留，但主要用于诊断。真正进入建模时，更倾向使用：

ratio
entropy
per_onset
per_ql
normalized histogram

原因是：不同作品长度、密度、体裁差异很大，如果直接比较 raw count，很容易把“作品更长/音符更多”误认为“风格不同”。

---

3. 每个特征家族回答不同的音乐问题

可以这样理解六个家族的分工：

音高：用了哪些音，音区在哪里？
音程：旋律线和低音线如何运动？
节奏：运动密度、时值结构和弱拍长音如何？
调性：音级分布、半音化和局部调性材料变化如何？
复调/织体：几个声部在活动，织体是否稳定，声部是否同步？
动机/模进：局部模式是否重复、移调、连续、自相似？
